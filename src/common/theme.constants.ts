const ReactInspector = require("react-inspector");
const { chromeLight } = ReactInspector;

export const THEME_OBJECT_INSPECTOR = {
    ...chromeLight,
    ARROW_FONT_SIZE: 14,
    BASE_FONT_SIZE: "14px",
    TREENODE_FONT_SIZE: "14px",
    TREENODE_LINE_HEIGHT: "16px"
};
